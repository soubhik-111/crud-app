/*
 * ProductData Messages
 *
 * This contains all the text for the ProductData container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProductData';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProductData container test!',
  },
});
